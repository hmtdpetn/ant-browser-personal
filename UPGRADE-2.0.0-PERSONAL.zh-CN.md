# Ant Browser Personal 2.0.0 升级说明

发布日期：2026-07-14
升级基线：个人版 1.3.0

2.0.0 是在个人版 1.3.0 主线继续演进的版本，不是用上游版本覆盖个人版。既有的个人代理连接栈、订阅兼容策略、自动化与实例迁移能力都会保留。

## 新增与改进

- 订阅 User-Agent：导入或刷新订阅时可选择内置 Ant、FlClash、Clash Verge、Clash for Windows UA，或填写自定义 UA；请求失败时可按已选策略回退轮换，并会把成功使用的 UA 保存到订阅来源中。
- 实例分组：实例列表改为树形分组，选择父分组会包含所有子分组，支持批量移动实例；实例、代理两侧栏均可折叠并记住状态。
- 代理分组：新增持久化的树形代理分组，支持子分组、新建、重命名、删除、删除时提升子项、按分组筛选，以及批量移动代理。旧版的 `group_name` 会在首次启动时自动迁移为根分组。
- 数据兼容：数据库会自动执行 v13 与 v14 架构迁移，保存订阅 UA/回退设置与代理树分组数据；备份合并同时兼容旧版和新版代理字段。
- 订阅刷新修复：刷新订阅后会保留代理所在的树形分组 ID，不会因为同名子分组而错误归入根分组。
- 界面：实例图标升级为多尺寸高清 ICO，`H1/H2` 以横向字母数字显示；分组弹窗不会被表头遮挡；实例表格只支持左右调整列宽，列宽会保存，行高不会改变。

## 从已有安装升级

1. 完全退出 Ant Browser，并确认浏览器实例也已停止。
2. 备份当前整个安装目录，重点是 `data/`、现有配置文件和 `bin/`。
3. 从 2.0.0 便携包中取出新的程序文件与运行时文件。对已有安装，保留原有 `data/` 和你自己的配置文件；不要用便携包中的空 `data/` 覆盖它们。
4. 启动 2.0.0。首次启动会自动执行 v13/v14 数据库迁移；正常完成后即可继续使用原有实例、代理和分组数据。

### 回退原则

数据库已经升级后，不建议直接用旧版程序打开同一份数据。需要回退时，请先退出程序，再恢复升级前备份的完整安装目录（程序文件、`data/` 和配置文件一起恢复）。

## 新安装

便携包可直接解压到一个新的空目录后运行。发布包只包含程序、必要运行时、示例配置、空的数据目录和说明文档；不包含任何用户数据库、浏览器资料、许可证、订阅地址或生产配置。

若发布包包含 `chrome/`，可直接使用内置 Chromium；若未包含，请按仓库 README 准备兼容的 Chromium 内核。安装后再在应用中创建实例或导入已有实例备份。

## 连接栈兼容性

个人版原有的代理连接栈规则仍然有效：

- `xray` 组合栈继续由 Xray 与 sing-box 分工处理相应协议；
- `mihomo` 为独立 Mihomo 栈；
- 实例启动、测速、连通性、IP 健康、预热与代理下载会按当前连接栈执行，不会在两套栈之间自动混用。

升级不会用上游的通用代理实现替换这些个人版兼容处理。

## 已完成的发布前验证

- `go mod verify`
- `go test ./... -count=1`
- TypeScript `tsc --noEmit`
- Vite production build
- Wails Windows/amd64 build
- `git diff --check`

以上检查均通过。Windows Defender 的自定义扫描在构建环境中因服务不可用而未执行；这不影响上述构建与测试结果。
