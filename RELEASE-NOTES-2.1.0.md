# Ant Browser Personal 2.1.0

发布日期：2026-08-18

本版本从个人版 2.0.1（`ab6ca5f`）升级，合入正式版最新源码（`10b36b5`）中的新增能力，同时保留个人版的分组、订阅与界面定制。

## 正式版新增能力

- 指纹自测、浏览器内检测、版本能力矩阵和自适应默认窗口参数。
- HTTPS 代理桥接、SOCKS5/链式代理修复，以及 Hysteria、Hysteria2、TUIC、AnyTLS 等协议解析增强。
- 实例历史标签恢复和实例内存上限。
- 实例数据目录快捷打开、删除数据审计和启动服务设置。
- 代理池测速、IP 健康、内核下载、日志与表格交互改进。
- 自动化示例、书签和备份相关修复与测试。

## 保留的个人版能力

- 实例和代理的树形分组、折叠侧栏、批量移动及快速新建分组。
- Clash 订阅 User-Agent 预设、自定义、失败回退和持久化。
- 分组 Launch HTTP API、任务栏实例徽标及个人版 UI 布局。
- 代理连接预热和个人版代理兼容逻辑。
- Xray 组合栈与 Mihomo 栈保持隔离，避免跨连接栈混用。

## 兼容性处理

- 数据库保留个人版迁移 13、14，并将正式版的历史标签恢复和内存限制顺延为迁移 15、16，可直接升级已有个人版数据。
- 实例编辑继续优先使用代理池；历史接口自定义代理在未重新选择代理时仍会保留。
- 后台进程同时执行个人版任务栏徽标设置和正式版内存限制清理。
- 订阅下载同时保留 UA 回退策略和正式版总超时保护。

## 产物

- 可执行文件：`build/bin/ant-chrome.exe`
- Windows 便携包：`publish/output/AntBrowser-2.1.0-windows-amd64-portable.zip`
- 便携包包含 `ant-chrome.exe`、`xray.exe`、`sing-box.exe`、初始化配置和空 `data` 目录。
- 仓库未包含可发布的 Chromium 内核，因此便携包仅附带 `chrome/README.md`；可在应用内下载内核，或复制原个人版的内核目录。

## 验证结果

- `go test ./...`：通过。
- TypeScript 与 Vite 生产构建：通过。
- Wails 2.12.0 Windows amd64 构建：通过。
- Windows 运行时文件哈希校验：通过。

旧个人版用户升级前建议备份 `data` 目录。需要沿用旧数据时，将原 `data` 目录复制到新版目录后再启动。
