# 个人版 1.3.0-personal.1 升级说明

本版本以 Ant Browser 上游 1.3.0 为基础，并保留个人主线已有的代理与隐私定制。

## 升级方式

- 覆盖程序文件前，请完整备份 `data/`、`config.yaml`、`bin/` 与旧版 EXE。
- 正式升级时保留原有 `data/` 和 `config.yaml`；首次启动会把本地数据库从旧架构迁移到当前架构。不要用上游或测试包的数据库覆盖自己的数据库。
- 迁移后不能直接降级使用旧程序；如需回退，请同时恢复升级前备份的程序和 `data/`。

## 上游新增能力

- 自动化脚本包导入、导出，以及本地文件、目录和 Git 来源导入。
- 浏览器实例包和完整备份导入导出，支持合并、重置与回收站恢复。
- 扩展包安装、Chrome Web Store 查询、扩展与实例绑定。
- 代理可按条目选择自动、Xray、sing-box 或 Mihomo 内核，并可按需下载和诊断内核。

Mihomo 是 Clash Meta 兼容内核，适合部分较新的 Clash 规则和协议。它是按需组件，不会随便携包默认下载；可在“代理池配置 → 下载内核”安装，再在相应代理编辑页选择它。

## 个人版保留行为

- 订阅拉取失败会按个人版的 User-Agent 轮询重试。
- AnyTLS 继续使用个人版的 Clash YAML 导入方案，不启用上游直接 `anytls://` URI 转换路径。
- VLESS Reality 保留空/无效 short-id 防护及个人版 IPv4 出站策略。
- 保留分组管理、实例任务栏图标标识、1280×720 默认窗口、私有化页面与旧配置兼容字段。
